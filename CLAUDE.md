# 멍냥맵 — 작업 규칙

한국관광공사 반려동물 동반여행 API 로 "우리 아이랑 여기 갈 수 있나?"에 답하는 서비스.
2026 관광데이터 활용 공모전 웹·앱 구현 부문 지정과제 6번.

아래 넷은 코드를 읽어서는 알 수 없고, 어겼을 때 되돌리기 어렵다.

## 1. 사용자 위치를 서버로 보내지 않는다

좌표를 서버로 **전송**하는 순간 저장 여부와 무관하게 위치기반서비스사업자 신고
대상이 된다(공모전 공지 FAQ). 신고 없이 운영하면 서비스 자체가 실격이다.

'내 주변'은 이렇게 우회한다 — `/api/places/coords` 가 좌표 색인만 내려주고,
브라우저가 `lib/geo.ts` 로 거리를 재고, 고른 `contentid` 만 서버에 되묻는다.
위치는 단말을 떠나지 않는다.

`/api/places` 에 `lat`·`lng` 류 파라미터를 추가하지 말 것. 지도 API 의 좌표 기반
검색(`locationBasedList2`)도 같은 이유로 쓰지 않는다.

## 2. 없는 데이터를 지어내지 않는다

방문자수·평점·리뷰·"인기 급상승" 같은 지표는 이 API 에 **없다**. 그럴듯하게
채우지 말고 "없다"고 쓴다.

- 핫플레이스는 인기순이 아니라 *조건이 빠짐없이 등록되고(A등급) 전 구역 동반
  가능한* 곳이다 (`lib/curate.ts`). 문구도 그 사실만 말해야 한다.
- 페스티벌은 전국 0건이라 `app/festival/page.tsx` 가 왜 비었는지를 적어 둔다.
  더미 데이터로 채우지 말 것.
- 판정 근거는 원문에서 나온 것만 쓴다 (`lib/petTour.ts`). 확인 못 한 것을
  초록 ✓ 로 찍으면 사용자가 준비 없이 출발한다.

헛걸음을 막으려고 만든 서비스가 헛걸음을 만들면 안 된다.

## 3. 출처 표기는 지운다는 선택지가 없다

화면에 데이터를 보여주는 곳마다 `출처 ⓒ한국관광공사` 가 붙어 있어야 한다.
공모전 필수 조건이고, 레이아웃을 정리하다 실수로 지우기 쉬운 자리에 있다.
`detailImage2` 의 Type3 이미지는 변경·크롭 금지다.

## 4. 커밋은 main 에 직접 한다

작업용 브랜치를 만들거나 PR 을 올리지 않는다. 혼자 쓰는 저장소라 브랜치와 머지가
순수한 번거로움이다. "커밋해서 올려" 를 들으면 `git add` → `git commit` →
`git push origin main`. 브랜치를 팔지 물어보지도 말 것.

## 참고

- `.env.local` 의 KTO·Supabase 키는 서버 라우트 전용이다. 클라이언트로 나가는 건
  `NEXT_PUBLIC_KAKAO_MAP_KEY` 뿐.
- `detailPetTour2` 는 장소당 1콜이고 벌크 조회가 없다. 개발계정 일 1,000건이라
  화면에 그려질 것만 조회한다 (`app/api/pet-rules/route.ts`).
- 테스트: `npm test` (판정 엔진 133개). 판정 로직을 고치면 여기부터 통과시킨다.
